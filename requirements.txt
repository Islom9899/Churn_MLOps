# ML / trening
pandas==2.3.3
scikit-learn==1.8.0
joblib==1.5.3
mlflow==3.13.0

# Trening pipeline orchestratsiyasi (Phase 2)
prefect==3.3.7         # flow va task orqali pipeline orchestratsiyasi
pyyaml==6.0.2          # DVC .dvc faylidan dataset hash o'qish uchun

# Ma'lumot validatsiyasi va versiyalash (Phase 1)
pandera==0.21.0        # sxema, diapason va kategoriya tekshiruvi
dvc==3.59.0            # dataset va model artefaktlarini versiyalash (git bilan bog'liq)
pathspec==0.12.1       # dvc 3.59 bilan mos versiya (1.1.x da import xatosi)
multimethod==1.9.1     # pandera 0.21 bilan mos versiya (2.x da 'overload' import xatosi)

# Xizmat (FastAPI)
fastapi==0.136.3
uvicorn[standard]==0.49.0

# Test
pytest==8.3.4
httpx==0.28.1

# Baza
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12
python-dotenv==1.2.2
